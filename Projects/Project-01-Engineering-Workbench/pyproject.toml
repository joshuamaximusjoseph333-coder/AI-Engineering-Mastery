[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "engineering-workbench"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
engineering-workbench = "engineering_workbench.cli:main"

[tool.setuptools.packages.find]
where = ["src"]